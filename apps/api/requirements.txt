# TokenCut API — core runtime
#
# Lower bounds only. Exact pins belong in requirements.lock.txt, generated by
# `pip freeze` after a real install — do not hand-write pinned versions.
#
#   pip install -r requirements.txt && pip freeze > requirements.lock.txt

fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.9
pydantic-settings>=2.6
anyio>=4.6

# Tokenizers
tiktoken>=0.8                # OpenAI — offline, exact, recoverable offsets
anthropic>=0.40              # Anthropic — count_tokens endpoint (keyed, no token charge)

# Optional providers. Import-guarded: the service boots and serves without them.
google-genai>=0.3            # Gemini — count_tokens (keyed, no token charge)

# Optional infra. Falls back to an in-process cache when REDIS_URL is unset.
redis[hiredis]>=5.2
